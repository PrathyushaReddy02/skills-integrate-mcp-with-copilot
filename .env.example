# Example environment variables for local development
# Copy this file to `.env` and fill in real values before running in development.

# General
DEBUG=False
ENVIRONMENT=development

# Secret used for teacher/admin features (change before production)
TEACHER_SECRET_KEY=change-me-in-production

# Admin user (for initial development only)
# To generate a bcrypt password hash in Python:
# >>> from passlib.context import CryptContext
# >>> pwd = CryptContext(schemes=["bcrypt"], deprecated="auto")
# >>> print(pwd.hash("your-admin-password"))
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=<paste-bcrypt-hash-here>
# Environment Configuration Example
# Copy this file to .env and fill in the actual values
# NEVER commit .env to version control

# Application Settings
DEBUG=False
ENVIRONMENT=development

# Teacher Authentication (for admin mode)
# Store teacher usernames and passwords securely
# For development only - in production use a proper auth system
TEACHER_SECRET_KEY=your-super-secret-key-change-this-in-production

# Database Configuration (future use)
# DATABASE_URL=sqlite:///./mergington_school.db

# API Security
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Logging
LOG_LEVEL=INFO
